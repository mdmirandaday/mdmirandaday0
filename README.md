# mdmirandaday0

A simple AutoHotkey script for remapping the Copilot key.
```
Copilot + Left Arrow = Previous Track
Copilot + Right Arrow = Next Track
Copilot = Play/Pause
```
You can make this script run at startup by `Win+R`, then typing `shell:startup`
